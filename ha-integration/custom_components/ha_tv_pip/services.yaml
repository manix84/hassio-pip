show_camera:
  fields:
    receiver_device_id:
      required: false
      selector:
        device:
          integration: ha_tv_pip
      name: Receiver device
      description: Paired Android TV or Google TV receiver to display the camera on.
    camera_entity:
      required: true
      selector:
        entity:
          domain: camera
      name: Camera entity
      description: Camera entity to display on the paired receiver. Use a TV-compatible H.264 or lower-resolution stream where possible.
    duration_seconds:
      default: 30
      selector:
        number:
          min: 1
          max: 3600
          mode: box
          unit_of_measurement: seconds
      name: Duration
      description: How long the receiver should keep the display open.
      example: 30
    enter_pip:
      default: true
      selector:
        boolean:
      name: Enter PiP
      description: Ask the receiver to use PiP or overlay mode when available.
      example: true
    snapshot_fallback:
      default: true
      selector:
        boolean:
      name: Snapshot fallback
      description: Show a camera snapshot while the video stream loads, when Home Assistant exposes one.
      example: true
    snapshot_camera_entity:
      required: false
      selector:
        entity:
          domain: camera
      name: Snapshot fallback camera entity
      description: Optional camera entity to use for the loading snapshot. Defaults to the main camera entity.
    stream_type:
      default: auto
      selector:
        select:
          options:
            - auto
            - hls
            - snapshot
      name: Stream type
      description: Choose automatic handling, force an HLS stream, or send a snapshot through the camera service.
      example: auto
    title:
      required: false
      selector:
        text:
      name: Title
      description: Optional display title. Defaults to the camera friendly name.
      example: Front Door
    message:
      required: false
      selector:
        text:
          multiline: true
      name: Message
      description: Optional smaller text to overlay on the camera popup.
      example: Motion detected at the front door.
    position:
      default: top_right
      selector:
        select:
          options:
            - top_right
            - top_left
            - bottom_right
            - bottom_left
      name: Position
      description: Corner where optional overlay text should appear.
      example: top_right
    title_color:
      default: "#50BFF2"
      selector:
        text:
      name: Title color
      description: Hex color for the title text when message overlay is used.
      example: "#50BFF2"
    title_size:
      default: 24
      selector:
        number:
          min: 10
          max: 48
          mode: box
      name: Title size
      description: Title text size when message overlay is used.
      example: 24
    message_color:
      default: "#fbf5f5"
      selector:
        text:
      name: Message color
      description: Hex color for the message text.
      example: "#fbf5f5"
    message_size:
      default: 18
      selector:
        number:
          min: 10
          max: 40
          mode: box
      name: Message size
      description: Message text size.
      example: 18
    background_color:
      default: "#B30F0E0E"
      selector:
        text:
      name: Background color
      description: Hex or alpha-hex color for the optional text overlay background.
      example: "#B30F0E0E"
    width:
      required: false
      selector:
        number:
          min: 240
          max: 1600
          mode: box
          unit_of_measurement: px
      name: Width
      description: Optional overlay width in pixels. Defaults to the receiver's sensible popup width.
      example: 720
    height:
      required: false
      selector:
        number:
          min: 120
          max: 900
          mode: box
          unit_of_measurement: px
      name: Height
      description: Optional overlay height in pixels. Media popups default to 640x360; text-only notifications default to content height.
      example: 405
show_snapshot:
  fields:
    receiver_device_id:
      required: false
      selector:
        device:
          integration: ha_tv_pip
      name: Receiver device
      description: Paired Android TV or Google TV receiver to display the snapshot on.
    camera_entity:
      required: true
      selector:
        entity:
          domain: camera
      name: Camera entity
      description: Camera entity to capture a snapshot from.
    duration_seconds:
      default: 10
      selector:
        number:
          min: 1
          max: 3600
          mode: box
          unit_of_measurement: seconds
      name: Duration
      description: How long the receiver should keep the snapshot open.
      example: 10
    enter_pip:
      default: true
      selector:
        boolean:
      name: Enter PiP
      description: Ask the receiver to use overlay mode for the snapshot.
      example: true
    title:
      required: false
      selector:
        text:
      name: Title
      description: Optional display title. Defaults to the camera friendly name.
      example: Front Door
    message:
      required: false
      selector:
        text:
          multiline: true
      name: Message
      description: Optional smaller text to overlay on the snapshot popup.
      example: Visitor snapshot captured.
    position:
      default: top_right
      selector:
        select:
          options:
            - top_right
            - top_left
            - bottom_right
            - bottom_left
      name: Position
      description: Corner where optional overlay text should appear.
      example: top_right
    title_color:
      default: "#50BFF2"
      selector:
        text:
      name: Title color
      description: Hex color for the title text when message overlay is used.
      example: "#50BFF2"
    title_size:
      default: 24
      selector:
        number:
          min: 10
          max: 48
          mode: box
      name: Title size
      description: Title text size when message overlay is used.
      example: 24
    message_color:
      default: "#fbf5f5"
      selector:
        text:
      name: Message color
      description: Hex color for the message text.
      example: "#fbf5f5"
    message_size:
      default: 18
      selector:
        number:
          min: 10
          max: 40
          mode: box
      name: Message size
      description: Message text size.
      example: 18
    background_color:
      default: "#B30F0E0E"
      selector:
        text:
      name: Background color
      description: Hex or alpha-hex color for the optional text overlay background.
      example: "#B30F0E0E"
    width:
      required: false
      selector:
        number:
          min: 240
          max: 1600
          mode: box
          unit_of_measurement: px
      name: Width
      description: Optional overlay width in pixels. Defaults to the receiver's sensible popup width.
      example: 720
    height:
      required: false
      selector:
        number:
          min: 120
          max: 900
          mode: box
          unit_of_measurement: px
      name: Height
      description: Optional overlay height in pixels. Media popups default to 640x360; text-only notifications default to content height.
      example: 405
show_notification:
  fields:
    receiver_device_id:
      required: false
      selector:
        device:
          integration: ha_tv_pip
      name: Receiver device
      description: Paired Android TV or Google TV receiver to display the notification on.
    title:
      default: Home Assistant
      selector:
        text:
      name: Title
      description: Larger notification heading text.
      example: Doorbell
    message:
      required: false
      selector:
        text:
          multiline: true
      name: Message
      description: Smaller notification body text.
      example: Someone is at the front door.
    duration_seconds:
      default: 15
      selector:
        number:
          min: 1
          max: 3600
          mode: box
          unit_of_measurement: seconds
      name: Duration
      description: How long the receiver should keep the notification open.
      example: 15
    enter_pip:
      default: true
      selector:
        boolean:
      name: Enter PiP
      description: Ask the receiver to use PiP or overlay mode when available.
      example: true
    position:
      default: top_right
      selector:
        select:
          options:
            - top_right
            - top_left
            - bottom_right
            - bottom_left
      name: Position
      description: Corner where the receiver should place the notification.
      example: top_right
    title_color:
      default: "#50BFF2"
      selector:
        text:
      name: Title color
      description: Hex color for the title text.
      example: "#50BFF2"
    title_size:
      default: 24
      selector:
        number:
          min: 10
          max: 48
          mode: box
      name: Title size
      description: Title text size.
      example: 24
    message_color:
      default: "#fbf5f5"
      selector:
        text:
      name: Message color
      description: Hex color for the message text.
      example: "#fbf5f5"
    message_size:
      default: 18
      selector:
        number:
          min: 10
          max: 40
          mode: box
      name: Message size
      description: Message text size.
      example: 18
    background_color:
      default: "#B30F0E0E"
      selector:
        text:
      name: Background color
      description: Hex or alpha-hex color for the notification background. Defaults to a translucent glass-style background.
      example: "#B30F0E0E"
    width:
      required: false
      selector:
        number:
          min: 240
          max: 1600
          mode: box
          unit_of_measurement: px
      name: Width
      description: Optional notification width in pixels. Defaults to 512 pixels.
      example: 512
    height:
      required: false
      selector:
        number:
          min: 120
          max: 900
          mode: box
          unit_of_measurement: px
      name: Height
      description: Optional notification height in pixels. Defaults to content height.
      example: 240
