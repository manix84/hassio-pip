{
  "name": "@ha-tv-pip/android-tv-app",
  "version": "0.2.0",
  "private": true,
  "description": "Android TV Kotlin receiver app for HA TV PiP.",
  "license": "MIT",
  "scripts": {
    "assemble": "./gradlew assembleDebug",
    "assemble:debug": "./gradlew assembleDebug",
    "assemble:release": "./gradlew assembleRelease",
    "clean": "./gradlew clean",
    "lint": "./gradlew lintDebug"
  }
}
